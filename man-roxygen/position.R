#  template used for position in add-functions. 
#' 
#' @param position character input giving the position for the new columns, can be one
#'      of c("first", "left", "right", "last", "keep"). The input can be abbreviated.
